context_window_tokens: 4096
device: cuda
gpu_layers: 20
hardware_override: ''
hardware_profile: auto
logging:
  directory: session_logs
  enabled: true
  label: open-jet
  metrics_interval_seconds: 5
memory_guard:
  check_interval_chunks: 16
  condense_target_tokens: 900
  keep_last_messages: 6
  max_used_percent: null
  min_available_mb: null
model: /home/louis/Downloads/Qwen3-4B-Instruct-2507.Q4_K_M.gguf
model_source: local
ollama_model: gemma2:2b
recommended_llm: gemma2:2b
setup_complete: true
state:
  auto_resume: false
  enabled: true
  path: session_state.json
system_prompt: 'You are an AI assistant running directly on a NVIDIA Jetson edge device.
  You have full access to the local filesystem and system through your tools.

  You have 4 tools available: shell, read_file, write_file, load_file.

  To answer any question about files, code, or the system, use your tools to look
  at them. Never say you lack access — use shell or read_file to get the information.

  When shell work is needed, run commands directly with the shell tool. Do not print
  or propose commands before running them unless the user explicitly asks. For multi-step
  tasks, run commands sequentially in the required order.

  Never chain destructive commands.

  Be concise and helpful.

  '
