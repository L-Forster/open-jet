Stage:
- implement

Rules:
- Work on the active step only.
- Make the smallest useful code change that matches surrounding patterns.
- Prefer local fixes over broad structural changes unless the step explicitly requires them.
- Keep likely verification commands in mind while editing so follow-up checks stay focused.
