{
  "case_id": "runtime_load_service_module",
  "description": "Load a runtime guard module into context before answering.",
  "prompt": "Use load_file on src/memory_guard.py, then explain what the module enforces. Do not use shell.",
  "template_dir": "edge_runtime_diagnostics",
  "mode": "chat",
  "allowed_tools": ["load_file"],
  "tags": ["runtime", "docs", "tool"],
  "expectations": [
    {"kind": "tool_used", "target": "load_file"},
    {"kind": "assistant_contains", "value": "memory"}
  ]
}
