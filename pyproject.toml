[project]
name = "open-jet"
version = "0.1.0"
description = "Minimal agentic TUI for running LLMs on edge devices"
requires-python = ">=3.10"
dependencies = [
    "textual>=3.0",
    "httpx>=0.27",
    "pyyaml>=6.0",
    "tiktoken>=0.7",
]

[project.scripts]
open-jet = "src.app:main"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["src*"]
